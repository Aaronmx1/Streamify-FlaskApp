<!-- base.j2 -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
</head>
<body>
    <header>
        <h1>Streamify Admin Panel</h1>
        <p>Use the navigation bar to access various sections of the platform.</p>
        <nav>
            <a href="{{ url_for('root') }}">Home</a>
            <a href="{{ url_for('/users') }}">Users</a>
            <a href="{{ url_for('/subscriptions') }}">Subscriptions</a>
            <a href="{{ url_for('/albums') }}">Albums</a>
            <a href="{{ url_for('/likedSongs') }}">Liked Songs</a>
            <a href="{{ url_for('/artists') }}">Artists</a>
            <a href="{{ url_for('/playlists') }}">Playlists</a>
            <a href="{{ url_for('/songs') }}">Songs</a>
            <a href="{{ url_for('/addedSongs') }}">Added Songs</a>
            <a href="{{ url_for('/collaborations') }}">Collaborations</a>
        </nav>
    </header>
    <main>
        {% block content %}{% endblock %}
    </main>
</body>
</html>